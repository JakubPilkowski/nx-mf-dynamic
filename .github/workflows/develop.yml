name: Develop CI

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

permissions:
  actions: read

jobs:
  ci:
    uses: ./.github/workflows/base.yml
    with:
      start-method: 'develop'
